<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Minimal platform theme; the app itself is styled entirely by Compose/Material3
         (see ui/theme/Theme.kt). This just sets the pre-Compose window background/chrome. -->
    <style name="Theme.OsmRide" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
